% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/pipeline.R
\name{generate_pipeline}
\alias{generate_pipeline}
\title{Generate a targets pipeline from a config}
\usage{
generate_pipeline(
  config,
  path = "_targets.R",
  report = TRUE,
  overwrite = FALSE,
  verbose = NULL
)
}
\arguments{
\item{config}{A list or a path to a YAML config file. Must contain a
\code{data.file} entry.}

\item{path}{Output path for the generated pipeline. Default is \code{"_targets.R"}.}

\item{report}{Install the report template and Quarto extension? Default is
\code{TRUE}.}

\item{overwrite}{Overwrite an existing \code{path}? Default is \code{FALSE}.}

\item{verbose}{Print progress messages? \code{NULL} (default) uses
\code{getOption("acme.toolkit.verbose", TRUE)}.}
}
\value{
Invisibly, the path to the generated pipeline.
}
\description{
Render a \verb{_targets.R} file from a config (a list or a path to a YAML file).
The config is validated, hashed, and the hash recorded in the file header so
\code{\link[=run_pipeline]{run_pipeline()}} can warn when the config has drifted. When \code{report = TRUE}
the Quarto report template and the Acme Quarto extension are installed
alongside the pipeline.
}
\examples{
config <- list(
  data = list(file = "demo.csv", name = "demo"),
  analyses = list(summary = list(method = "describe"))
)
path <- file.path(tempdir(), "_targets.R")
generate_pipeline(config, path = path, report = FALSE, overwrite = TRUE)

}
