% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/aaa-generics.R
\name{filter}
\alias{filter}
\title{Filter rows of a dataset}
\usage{
filter(object, ...)
}
\arguments{
\item{object}{A \link{Dataset} or \link{Project} object, or any object supported by a
masked \code{filter()} elsewhere on the search path.}

\item{...}{For acme.toolkit classes: an unquoted predicate evaluated in the
data (e.g. \code{mpg > 20}). For other objects: forwarded to the masked
function.}
}
\value{
A filtered \link{Dataset} or \link{Project}, or the result of the masked
function.
}
\description{
\code{filter()} is an S7 generic that keeps rows of a \link{Dataset} (or the data of a
\link{Project}) matching a predicate. The generic intentionally shares its name
with \code{\link[dplyr:filter]{dplyr::filter()}} and \code{\link[stats:filter]{stats::filter()}}; for any object that is not an
acme.toolkit class, the call is forwarded to the next \code{filter()} on the
search path. This is the "masked verb" pattern: the package can offer an
ergonomic verb without breaking existing code that relies on the base or
tidyverse function of the same name.
}
\examples{
d <- Dataset(data = mtcars, name = "mtcars")
filter(d, cyl == 4)

}
